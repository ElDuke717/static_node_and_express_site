extends layout.pug

block content
  
  article.portfolio-error 

    h1 Uh oh, something's not right.
    h2= error.status
    h2= error.message
    h3 Stack trace:
    
    .stack-trace
        p= error.stack